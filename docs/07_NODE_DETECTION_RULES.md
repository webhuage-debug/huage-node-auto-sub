# 07 节点检测规则

## 文档目的

定义节点可用性检测的第一阶段目标。

## 当前版本范围

v0.3.0 会把解析进入节点池的节点统一标记为 `status=untested`，默认地区为 `未知`。

当前不执行真实可用性检测，不判断延迟，不调用 Xray-core、sing-box 或 Mihomo。

## 后续待补充内容

v0.4.0 补充 Xray-core 检测流程、超时策略、并发限制、失败原因和检测结果入池规则。
